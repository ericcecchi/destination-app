<!DOCTYPE html>
<html>
<head>
  <title><%= full_title(yield(:title)) %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <link rel="stylesheet" href="https://rsms.me/inter/inter.css">
  <%= stylesheet_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body class="bg-white dark:bg-gray-900 text-gray-700 dark:text-gray-200 relative">
  <%= render 'layouts/header' %>
  <% unless flash.empty? %>
    <div id="flash-container" class="max-w-7xl mx-auto">
      <% flash.each do |message_type, message| %>
        <%= content_tag(:div, message, class: "m-3 px-4 py-3 rounded-lg shadow-lg mb-4 text-sm #{flash_styles message_type}") %>
      <% end %>
    </div>
  <% end %>

  <%= yield %>
  <%= render 'layouts/footer' %>
</body>
</html>
